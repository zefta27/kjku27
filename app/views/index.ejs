<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= title %></title>
  </head>
  <body>
    <h1><%= title %></h1>
    <% if (userFullName) { %>
      <h2>Hello <%=userFullName%></h2>
      <a href="/signout">Sign Out</a>
    <% }else{ %>
      <a href="/signup">Sign Up</a>
      <a href="/signin">Sign In</a>
    <% } %>
  </body>
</html>
